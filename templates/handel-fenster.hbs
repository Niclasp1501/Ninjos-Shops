{{!--
  Was jemand dem Spieler hingelegt hat.

  Kein Laden, kein Regal, keine Auslage — eine Person hält etwas hin. Deshalb
  steht oben ihr Gesicht und nicht ein Ladenschild, und deshalb gibt es keine
  Bestandsspalte: Was hier steht, ist genau das, was gemeint ist.
--}}
<div class="shops-teil-handel">

  {{#if handel}}
    <header class="shops-handel-kopf">
      {{#if handel.personBild}}<img src="{{handel.personBild}}" alt="">{{/if}}
      <div>
        <strong>{{handel.personName}}</strong>
        {{#if handel.satz}}<span class="shops-handel-satz">„{{handel.satz}}"</span>{{/if}}
      </div>
    </header>

    {{#if figurName}}
      <div class="shops-boerse">
        <i class="fa-solid fa-coins"></i>
        <span class="shops-boerse-label">{{figurName}}</span>
        <strong>{{boerse}}</strong>
      </div>
    {{/if}}

    {{#if handel.posten.length}}
      <ol class="shops-ware shops-handel-liste">
        {{#each handel.posten}}
          <li class="shops-zeile" data-item-id="{{itemId}}">
            {{!-- Bild und Name führen zur Nahansicht — wie im Laden. Wer nicht
                  weiß, was ihm hingehalten wird, kauft es auch nicht. --}}
            {{#if img}}<img src="{{img}}" alt="" data-action="ansehen"
                            title="{{localize 'SHOPS.Ansehen.Knopf'}}">
            {{else}}<i class="fa-solid fa-cube shops-kein-bild"></i>{{/if}}
            <div class="shops-name">
              <span class="shops-warenname" data-action="ansehen"
                    title="{{localize 'SHOPS.Ansehen.Knopf'}}">{{name}}</span>
            </div>
            <span class="shops-menge">{{menge}}&times;</span>
            <span class="shops-preis">{{summeText}}</span>
            <button type="button" class="shops-kaufen" data-action="nehmen">
              <i class="fa-solid fa-hand-holding"></i>
              {{localize "SHOPS.Handel.Nehmen"}}
            </button>
          </li>
        {{/each}}
      </ol>
    {{else}}
      <p class="shops-leer shops-hinweisfeld">{{localize "SHOPS.Handel.NichtsVorgelegt"}}</p>
    {{/if}}

    {{!-- Eine Fußleiste, nicht zwei. Beides ist dieselbe Frage — was mache ich
          mit dem, was mir hingehalten wird — und stand vorher in zwei
          getrennten Kästen untereinander.

          Ohne Ablehnen bliebe das Fenster stehen, bis die Spielleitung es
          zurücknimmt — und die merkt nicht, dass der Spieler längst weiter
          ist. Ein Angebot, das man nur annehmen oder ignorieren kann, ist
          keine Frage, sondern eine Auslage. --}}
    <footer class="shops-handel-fuss">
      <div class="shops-handel-knoepfe">
        {{#if handel.nimmtAn}}
          <button type="button" class="shops-kaufen" data-action="anbieten">
            <i class="fa-solid fa-hand-holding"></i>
            {{localize "SHOPS.Handel.Anbieten"}}
          </button>
        {{/if}}
        <button type="button" class="shops-kaufen shops-zweit" data-action="ablehnen">
          <i class="fa-solid fa-hand"></i>
          {{localize "SHOPS.Handel.Ablehnen"}}
        </button>
      </div>
      {{#if handel.nimmtAn}}
        <span class="shops-blockhinweis">{{localize "SHOPS.Handel.AnbietenHinweis"}}</span>
      {{/if}}
    </footer>
  {{else}}
    <p class="shops-leer shops-hinweisfeld">{{localize "SHOPS.Handel.NichtsVorgelegt"}}</p>
  {{/if}}
</div>
